PORT=5000
NODE_ENV=production

# Dung khi deploy len Neon/Supabase/Render PostgreSQL.
DATABASE_URL=
DB_SSL=true

# Dung khi chay local bang PostgreSQL tren may.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_NAME=dental_clinic_management

JWT_SECRET=change_this_to_a_long_random_secret
CLIENT_URL=http://localhost:5173

GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
GEMINI_TIMEOUT_MS=5000
